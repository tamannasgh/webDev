alert("hey");
//alert is a modal-window that gave a msg to the user and until the user deal with it they can't interact with the page.

let age = prompt("what's your age?", 15);
alert(age);
//prompt is input field in js and the second parameter is optional (the default value) but in ie it's mendatory but if u don't wanna give you can do it with prompt("what's your age?", '');

let sure = confirm("you really wanna delete this?");
alert(sure)
// The function confirm shows a modal window with a question and two buttons: OK and Cancel.
// The result is true if OK is pressed and false otherwise.

