const naam = "Tamanna Sharma";
console.log(naam + " is a const variable mans it will initialise only once now its value can not be changed, will give error!");

let age = 15;
console.log(age + " is variables decalred using let can be reasigned or initialise easily my age ganna increase every year!");

console.log("there's one more way using var but scoping with this is bhit bekar so will discuss later but for now just don't use it everything can be done with let and const easily!");
